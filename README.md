# Currency Converter app

A Flutter app that converts currencies (BRL/USD/EUR).

*Note: You should create a key file("key.dart") apart that contains the URL requested(http request). In this code I call it as "request"(Line 50 - home_page.dart).
